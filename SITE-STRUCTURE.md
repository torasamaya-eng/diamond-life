# 公開前のサイト構成

- 初回はトップを表示。ニューゲームから既存の選手設定を開く。既存オートセーブがある場合のみ「続きから」を表示する。
- 選手名鑑の一覧・詳細は全画面のSPA表示。`sitePage` は表示状態だけを管理し、保存データへ加えない。履歴の戻る・進むに対応し、再読込みは安全にトップへ戻る。
- 詳細は保存済みsnapshotを既存のcareer/table等で描画する。閲覧後は進行中の選手参照を復元し、オートセーブや乱数に触れない。
- プライバシー・規約・お問い合わせは既存の独立HTMLを使用。同じタブで開き、各ページからトップへ戻れる。
- 引退後キャリアの末尾に「結果をシェア」、その下に「この選手を保存」を配置。保存上限・重複防止・削除確認・入替・容量エラーは既存処理を使用する。
- 新しい画面のCSSは `.site-*` に限定。ゲーム本編のレイアウト・ゲームロジック・広告・共有・保存形式は変更しない。

## 検証

`node build-single-file.mjs` / `npm test`

`node tests/site-browser-check.mjs` は独立したEdgeのブラウザコンテキストでPC1280px・スマホ390px/320pxを確認する。実ユーザーのストレージは使用しない。Playwrightの場所は `PLAYWRIGHT_MODULE` で指定可能。ローカルサーバーは `npm start`。

既存198件に5件追加。初回自動設定画面と名鑑モーダルを前提にした検査は新しい導線へ更新。過去の保護ハッシュとaudit baselineは再生成せず、今回変更を依頼されたUIのみ過去のUIハッシュ比較から除外。ゲーム全モジュールは作業前ハッシュで別途検証する。

## 公開前の未設定事項

お問い合わせ窓口は未設定のまま。実際の連絡先を `dist/src/public-config.js` に設定する。広告SDK・ID・同意処理等は今回の対象外であり、導入時に実際の公開環境に合わせて案内を更新する。
